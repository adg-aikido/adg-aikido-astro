const colors = require("tailwindcss/colors");

module.exports = {
    theme: {
        colors: {
            gray: colors.slate,
            transparent: colors.transparent,
            white: colors.white,
            black: colors.black,

            primary: colors.sky,
            secondary: colors.emerald,
            accent: colors.amber,

            error: colors.red,
            ok: colors.green,
            info: colors.blue,
        },
    },
};

