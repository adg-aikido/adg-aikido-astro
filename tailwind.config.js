const colors = require("tailwindcss/colors");

module.exports = {
    theme: {
        colors: {
            gray: colors.slate,
            transparent: colors.transparent,
            white: colors.white,
            black: colors.black,
            primary: colors.blue,
            accent: colors.violet,
            error: colors.red,
            ok: colors.green,
        },
    },
};
