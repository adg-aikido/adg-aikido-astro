---
import { getCollection } from 'astro:content';

const instructorsCollection = await getCollection('instructors');
const instructors = instructorsCollection.map(i => i.data);
---

<section class="py-20 px-4">
  <div class="max-w-6xl mx-auto">
    <h2 class="text-center font-serif text-4xl md:text-5xl font-bold mb-4">
      Guidat av experter, stödd av gemenskap
    </h2>
    <p class="text-center opacity-70 text-lg mb-16 max-w-2xl mx-auto">
      Våra mycket erfarna instruktörer är dedikerade till din tillväxt och framgång.
    </p>

    <!-- Instructors Grid -->
    <div class="grid md:grid-cols-3 gap-8 mb-16">
      {instructors.map((instructor) => (
        <div class="text-center">
          <div class="w-40 h-40 bg-primary/20 rounded-full mx-auto mb-4 flex items-center justify-center overflow-hidden">
            {instructor.image ? (
              <img
                src={instructor.image}
                alt={instructor.name}
                class="w-full h-full object-cover"
              />
            ) : (
              <span class="text-6xl">👤</span>
            )}
          </div>
          <h3 class="font-serif text-xl font-bold mb-2">{instructor.name}</h3>
          <p class="text-primary font-semibold mb-2">{instructor.rank}</p>
          {instructor.description && (
            <p class="opacity-70 text-sm">
              {instructor.description}
            </p>
          )}
        </div>
      ))}
    </div>

    <!-- Video and Image Section -->
    <div class="grid md:grid-cols-2 gap-8 mb-16 items-center">
      <!-- Video -->
      <div class="bg-base-200 text-base-content rounded-lg overflow-hidden shadow-lg">
        <div class="relative w-full aspect-video">
          <iframe
            class="absolute inset-0 w-full h-full"
            src="https://www.youtube.com/embed/uQBk769BXp8"
            frameborder="0"
            loading="lazy"
            allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen
          ></iframe>
        </div>
      </div>

      <!-- Image -->
      <div class="rounded-lg overflow-hidden shadow-lg">
        <img
          src="/assets/NikkyoHands.JPG"
          alt="Aikido training session"
          class="h-79 "
        />
      </div>
    </div>

    <!-- Social Proof -->
    <div class="grid md:grid-cols-3 gap-8 text-center">
      <div>
        <div class="text-5xl font-bold text-primary mb-2">4.8/5</div>
        <p class="opacity-70">⭐⭐⭐⭐⭐ (47 recensioner)</p>
      </div>
      <div>
        <div class="text-5xl font-bold text-primary mb-2">200+</div>
        <p class="opacity-70">Aktiva medlemmar</p>
      </div>
      <div>
        <div class="text-5xl font-bold text-primary mb-2">20+</div>
        <p class="opacity-70">Klasser varje vecka</p>
      </div>
    </div>
  </div>
</section>
