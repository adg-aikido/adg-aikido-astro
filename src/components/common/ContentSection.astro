---
export interface Props {
  title: string;
  id?: string;
  class?: string;
}

const { title, id, class: className } = Astro.props;
---

<section class:list={["overflow-auto px-2 pt-4 pb-8 prose prose-headings:font-serif max-w-none dark:prose-invert", className]}>
  <h2 class="mt-6 mb-6 text-3xl" id={id}>
    <span class="inline-block border-b-4 border-primary/70 pr-12">{title}</span>
  </h2>
  <div class="space-y-5">
    <slot />
  </div>
</section>
